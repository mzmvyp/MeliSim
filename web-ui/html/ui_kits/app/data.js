// MeliSim — DEPRECATED.
// All UI data now comes from the live API via api.js.
// Stub kept so any stray <script> reference doesn't 404.
console.warn("[MeliSim] data.js is empty — UI uses api.js now.");
